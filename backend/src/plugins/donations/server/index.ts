import controllers from "./controllers";
import services from "./services";

export default {
  controllers,
  services,
  // Note: Routes are defined in src/api/donation/routes/custom-donation-routes.ts
  // to maintain backward compatibility with /api/* URLs
};
