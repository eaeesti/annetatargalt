# Test Environment Variables
# Used when running integration tests (yarn test:integration)
#
# Copy this file to .env.test and adjust DATABASE_USERNAME and DATABASE_PASSWORD
# for your local PostgreSQL setup.

# Database credentials (same as development)
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=

# Test database name (separate from development)
DRIZZLE_DATABASE_NAME=annetatargalt_donations_test

# Enable integration tests
INTEGRATION_TESTS=true
